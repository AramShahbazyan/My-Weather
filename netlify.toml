[build]
  publish = "."
  functions = "netlify/functions"

# Makes the function reachable at the clean path /api/weather
# instead of /.netlify/functions/weather
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
